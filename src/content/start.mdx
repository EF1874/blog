export const metadata = {
  title: 'start',
  description: 'Demonstrating multiple posts',
  date: '2025-03-18T12:00:00Z',
  tags: ['writing'],
}

## start1

nextjs react query blog
```ts
const a: string = '1';
function fun () {
    let b = {
        a: 1,
        b: 2
    }

    console.log(a, b.a, b.b)
}
```
